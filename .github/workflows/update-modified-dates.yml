name: Update Modified Dates

# Keeps every page's freshness metadata pinned to the commit date of the last
# real (non-bot) change to that file:
#   * blog posts  — JSON-LD dateModified + the single visible "last updated"
#                   line after the references + sitemap lastmod;
#   * index.html and blog/index.html — JSON-LD dateModified (home page only)
#                   and sitemap lastmod. No visible date: Google never shows
#                   one for a non-article page.
# Every sitemap entry is now generated, so none of them needs hand-editing.
#
# Two details keep this from looping:
#   * the commit below carries "[skip ci]", so the bot's own push does not
#     re-trigger this workflow;
#   * the script ignores "[skip ci]" commits when looking for the last real
#     change, so re-running yields the same date, hence no diff, hence no
#     commit. Bot commits can never bump the date on their own.

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: site-data
      cancel-in-progress: false
    steps:
      # fetch-depth 0 is required: the script walks back through commit
      # history to find the last non-bot change, and a shallow clone would
      # only ever see one commit.
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Refresh modified dates
        run: python scripts/update_modified_dates.py

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html blog/unified-fid.html blog/unified-fid-en.html sitemap.xml
          if git diff --cached --quiet; then
            echo "No change, skipping commit."
          else
            git commit -m "meta: refresh modified dates [skip ci]"
            git pull --rebase origin main
            git push
          fi
